SECRET_KEY=change-me-to-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Database (Render auto-injects DATABASE_URL for PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost/maybankpdf2json

# Render deployment (auto-injected by render.yaml)
# CSRF_TRUSTED_ORIGINS=https://your-render-app.onrender.com
# RENDER_EXTERNAL_HOSTNAME=your-render-app.onrender.com
